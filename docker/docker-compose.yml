version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: redforge
      POSTGRES_PASSWORD: redforge
      POSTGRES_DB: redforge
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

  teamserver:
    build:
      context: ../teamserver
      dockerfile: ../docker/teamserver/Dockerfile
    environment:
      REDFORGE_PORT: "9080"
      DATABASE_URL: "postgres://redforge:redforge@postgres:5432/redforge?sslmode=disable"
      REDFORGE_JWT_SECRET: "super-secret-key"
      REDFORGE_ADMIN_USER: "admin"
      REDFORGE_ADMIN_PASS: "redforge-admin"
    depends_on:
      - postgres
    ports:
      - "9080:9080"

  ui:
    build:
      context: ../ui
      dockerfile: ../docker/ui/Dockerfile
    ports:
      - "5174:5173"
    environment:
      VITE_TEAMSERVER_URL: "http://teamserver:9080"

volumes:
  postgres_data:
