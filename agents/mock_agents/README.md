# Mock Agents

Mock agent implementations and fixtures used for:
- UI development (TUI/Web)
- Integration tests (agent ↔ server protocol)

These agents must remain **simulation-only** and should not execute commands on the host.

