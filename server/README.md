# Server

Teamserver backends for the **RedForgeC2 simulation lab**.

- `python_backend/`: Python skeleton (e.g., FastAPI) for local-only simulation.
- `go_backend/`: Go skeleton (e.g., Echo/Gin) for local-only simulation.

All endpoints and transports must be safe and constrained to lab environments.

